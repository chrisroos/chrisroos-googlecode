%h1 All domains

%p
  = link_to 'New domain', new_admin_domain_path

%table.domains
  %thead
    %tr
      %th domain
      %th domain hash
  %tbody
    - @domains.each do |domain|
      %tr
        %td= h(domain.domain)
        %td= h(domain.domain_hash)